fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
celery>=5.4
redis>=5.0
langchain>=0.2
langchain-core>=0.2
langchain-openai>=0.1
pyjwt>=2.8
# AES-256-GCM for encryption at rest of sensitive fields
# (src/infrastructure/security/). The only cryptographic primitive this
# codebase uses directly; nothing here implements its own.
cryptography>=43.0
anthropic>=0.40
httpx>=0.27
python-multipart>=0.0.9
pypdf>=4.2
python-docx>=1.1
# Browser automation for application portals. The wheel is only the driver;
# the Chromium build it drives is installed separately with
# `playwright install chromium` (see `make install`).
playwright>=1.44
